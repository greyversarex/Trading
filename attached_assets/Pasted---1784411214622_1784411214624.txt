Проведи полный технический аудит этого проекта. Мне нужна ТОЛЬКО правда из реального кода — не описания из README, не предположения, не маркетинговые формулировки. Каждое утверждение подтверждай ссылкой на файл и функцию. Если чего-то нет в коде — пиши «НЕ РЕАЛИЗОВАНО». Если что-то является заглушкой, моком или хардкодом — пиши «ЗАГЛУШКА» и показывай где.
Структура отчёта:
1. Архитектура

Полное дерево файлов проекта с назначением каждого файла (одной строкой)
Стек: язык, фреймворки, все библиотеки из зависимостей и ЗАЧЕМ каждая используется (если библиотека установлена, но не используется — отметь)
Как запускается проект, какие процессы работают одновременно (сервер, воркеры, cron и т.д.)

2. Источники данных

Откуда берутся данные графиков: какая биржа/API, какие эндпоинты, REST или WebSocket
Какие таймфреймы и какие пары реально запрашиваются (покажи конкретный код)
Как часто обновляются данные в «лайв» режиме (реальный интервал в секундах из кода)
Есть ли rate limits, обработка ошибок API, реконнекты — или при падении API всё умирает
Хранятся ли данные (БД, файлы, только память?) и сколько истории доступно

3. Модуль поиска графических паттернов

Список ВСЕХ паттернов, которые реально детектируются (не список из комментариев, а те, для которых есть работающая логика)
Для каждого паттерна: точный алгоритм детекции — какие условия, какие пороговые значения, покажи код
Это математическая логика (правила на свечах/пивотах) или ML-модель? Если ML — какая модель, на чём обучена, где веса
Как определяется что паттерн «завершён» vs «формируется»
Есть ли оценка качества/уверенности паттерна, и как она считается

4. Модуль поиска по скриншотам (visual similarity)

Как обрабатывается загруженный скриншот: OCR? CV? извлечение свечей? эмбеддинги изображений? Покажи пайплайн шаг за шагом
Как скриншот превращается в сравнимый формат и с чем именно сравнивается
Какая метрика схожести используется (DTW, косинусное расстояние, корреляция, что-то ещё) и покажи формулу/код
Какие пороги «похожести» стоят и откуда взялись эти числа
Работает ли это в лайве реально: с какой частотой прогоняется сравнение по каким парам/таймфреймам

5. Лайв-режим и уведомления

Что реально происходит в реальном времени, а что по запросу
Как пользователь узнаёт о находке: пуш, телеграм, просто список в UI?
Задержка от появления паттерна на бирже до отображения у пользователя (оцени по коду)

6. Фронтенд

Какие страницы/экраны есть, что на каждом работает, что декоративное
Чем рисуются графики (библиотека) и что на них реально отображается

7. Честная оценка

Топ-10 самых слабых мест проекта: где логика примитивная, где заглушки, где всё сломается под нагрузкой
Что из заявленного функционала работает на 100%, что частично, что вообще не работает
Какие данные/расчёты могут давать ложные сигналы и почему

Не сокращай, не приукрашивай. Отчёт нужен для принятия решений о деньгах.